---
title: 信息层级，不必靠边框一层套一层
blurb: 三层信息一层套一层的边框会很挤；字号、颜色、留白，或背景色的深浅同样能表达层级，而且更轻。
date: 2026-07-24
category: layout
tags:
  - hierarchy
  - visual-design
  - css
---

import DemoFrame from '@/components/lab/DemoFrame.astro'
import InfoHierarchyDemo from './InfoHierarchyDemo.tsx'

<DemoFrame bleed>
  <InfoHierarchyDemo client:visible />
</DemoFrame>

## 一份数据，三种渲染

同一份数据——任务 → 步骤 → 日志，三层信息——渲染三遍：

- **反例（三层嵌套边框）**：读者要先分辨三种边框的粗细和颜色，才能意会“这是层级、不是并列内容”——边框在替读者做本该由排版做的事。
- **正例 A（字号 · 颜色 · 留白）**：标题大而深、步骤中等、日志小而浅；边框数量为零，层级反而一眼可辨。
- **正例 B（背景色差）**：不画一条线，步骤块是任务色的 7% 浓度、日志块是 13% 浓度——颜色越浓，说明嵌套得越深。

## 为什么成立

1. 边框在暗示“这是一个独立区域”。三层嵌套边框，等于给读者三个不必要的视觉边界。
2. 字号、字重、颜色的深浅对比，同样能传达“谁主谁次”，而且视线扫过去更轻。
3. 留白比边框更安静地分组：靠得近的是一组，留白大的地方，自然就是两组。
4. 背景色的浓淡也是层级信号：颜色越浓、越靠近观感“前景”，天然读作“嵌套得更深”，不需要一条线来宣告。

## 什么时候仍然该用边框

不是说边框有罪。当两块内容**真的是并列、需要清晰切分**（比如表格单元、可点击的独立卡片），一条边框是最直接的表达。要避免的是用边框去表达“从属 / 嵌套”——那本该交给字号、留白和背景色。
