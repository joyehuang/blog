---
import { Image } from 'astro:assets'
import avatar from 'src/assets/avatar.png'

import IntroOverlay from '@/components/intro/IntroOverlay.astro'
import JoJo from '@/components/mascot/JoJo'

type FeedItem = {
  title: string
  href: string
  meta: string
  kind: string
}

type RepoItem = {
  name: string
  description: string
  stars: number
}

interface Props {
  locale: 'zh' | 'en'
  posts: FeedItem[]
  notes: FeedItem[]
  repos: RepoItem[]
}

const { locale, posts, notes, repos } = Astro.props
const isEn = locale === 'en'
const prefix = isEn ? '/en' : ''

const copy = isEn
  ? {
      work: 'Work',
      journal: 'Journal',
      about: 'Evidence',
      contact: 'Contact',
      role: 'AI agent & product engineer',
      intro: 'I turn difficult AI systems into products people can actually use.',
      location: 'Melbourne · Shanghai · Remote',
      explore: 'Enter the work',
      selected: 'Three products. Three different systems.',
      selectedBody:
        'Every frame below comes from the product itself. Scroll to move through the interface, not a mood board.',
      visit: 'Visit product',
      evidence: 'Beyond the prompt, leave evidence.',
      evidenceBody:
        'Engineering judgement becomes more useful when it is written, taught, and released. These are real outputs from that practice.',
      evidenceCta: 'Open the full talk',
      journalTitle: 'Notes from the build',
      journalBody: 'The decisions, failures, and patterns that survived contact with production.',
      essays: 'Essays',
      notes: 'Notes',
      openSource: 'Open source',
      sourceBody: 'Working implementations, not architecture theatre.',
      finale: 'Bring the difficult version.',
      finaleBody:
        'Context, constraints, and the outcome you need. An unfinished brief is enough to start.',
      context: 'Context',
      constraint: 'Constraint',
      outcome: 'Desired outcome',
      contextValue: 'What is true today?',
      constraintValue: 'What cannot be compromised?',
      outcomeValue: 'What should change for the user?',
      ready: 'Ready to shape',
      talk: 'Send the rough brief',
      replay: 'Replay entrance',
      copyright: 'Designed and engineered by Joye.'
    }
  : {
      work: '作品',
      journal: '写作',
      about: '证据',
      contact: '联系',
      role: 'AI Agent 与产品工程师',
      intro: '把复杂的 AI 系统，做成真正有人愿意使用的产品。',
      location: '墨尔本 · 上海 · 远程',
      explore: '进入作品',
      selected: '三个产品，三套不同的系统。',
      selectedBody: '下面每一帧都来自产品本身。滚动进入真实界面，而不是气氛图。',
      visit: '访问产品',
      evidence: 'Prompt 之外，留下证据。',
      evidenceBody:
        '工程判断只有被写下来、讲出来、做成开源实现，才会继续产生价值。这些是实际产物，不是装饰。',
      evidenceCta: '查看完整分享',
      journalTitle: '来自开发现场',
      journalBody: '记录真正进入生产之后，仍然成立的判断、失败与方法。',
      essays: '文章',
      notes: '笔记',
      openSource: '开源',
      sourceBody: '可以运行的实现，而不是架构表演。',
      finale: '把难的版本发来。',
      finaleBody: '背景、约束、想达成的结果。一个还没整理好的 brief，就足够开始。',
      context: '背景',
      constraint: '约束',
      outcome: '成功标准',
      contextValue: '今天真实发生了什么？',
      constraintValue: '什么绝对不能被牺牲？',
      outcomeValue: '用户最终应该感受到什么变化？',
      ready: '可以开始塑形',
      talk: '发送粗糙版本',
      replay: '重播入场',
      copyright: 'Joye 设计与开发。'
    }

const projects = isEn
  ? [
      {
        index: '01',
        key: 'playyy',
        title: 'Playyy',
        role: 'Brand-aware AI image workspace',
        body: 'A creative environment that keeps brand context, generation, and iteration in one product surface.',
        href: 'https://playyy.ai/',
        image: '/images/landing/playyy-ui.png',
        alt: 'Playyy product interface from the public website',
        domain: 'playyy.ai',
        accent: '#659eb9'
      },
      {
        index: '02',
        key: 'atypica',
        title: 'atypica',
        role: 'Commercial research multi-agent system',
        body: 'Clarification, persona interviews, and analysis are coordinated into a research workflow that can recover from asynchronous tool work.',
        href: 'https://atypica.ai/',
        image: '/images/landing/atypica-site.png',
        alt: 'Atypica public website showing the research agent interface',
        domain: 'atypica.ai',
        accent: '#18f52f'
      },
      {
        index: '03',
        key: 'aixcut',
        title: 'AIXCut',
        role: 'Agent-assisted video editing',
        body: 'A video workflow where the agent helps shape scripts, locate edits, and turn a large source library into a finished cut.',
        href: 'https://aixcut.cn/',
        image: '/images/landing/aixcut-site.png',
        alt: 'AIXCut public website showing the video editing product',
        domain: 'aixcut.cn',
        accent: '#28ffa9'
      }
    ]
  : [
      {
        index: '01',
        key: 'playyy',
        title: 'Playyy',
        role: '品牌感知的 AI 图像工作台',
        body: '把品牌上下文、图像生成与迭代放进同一个真实创作界面。',
        href: 'https://playyy.ai/',
        image: '/images/landing/playyy-ui.png',
        alt: 'Playyy 官网公开的产品界面',
        domain: 'playyy.ai',
        accent: '#659eb9'
      },
      {
        index: '02',
        key: 'atypica',
        title: 'atypica',
        role: '商业研究多智能体系统',
        body: '把澄清式反问、Persona 访谈与分析组织成可恢复的异步研究工作流。',
        href: 'https://atypica.ai/',
        image: '/images/landing/atypica-site.png',
        alt: 'Atypica 官网展示的研究 Agent 产品界面',
        domain: 'atypica.ai',
        accent: '#18f52f'
      },
      {
        index: '03',
        key: 'aixcut',
        title: 'AIXCut',
        role: 'Agent 辅助视频剪辑',
        body: '让 Agent 参与脚本塑形、定位剪辑与素材组织，把庞大的源视频推进到可交付成片。',
        href: 'https://aixcut.cn/',
        image: '/images/landing/aixcut-site.png',
        alt: 'AIXCut 官网展示的视频剪辑产品界面',
        domain: 'aixcut.cn',
        accent: '#28ffa9'
      }
    ]

const evidence = isEn
  ? [
      {
        label: 'Talk / Engineering scope',
        title: 'What agent engineering actually contains',
        image: '/talks/2026-from-interview-to-agent/engineering-scope.png'
      },
      {
        label: 'Method / Growth loop',
        title: 'A repeatable loop from interview to production',
        image: '/talks/2026-from-interview-to-agent/growth-loop.png'
      },
      {
        label: 'Practice / Agent legwork',
        title: 'The unglamorous work behind reliable agents',
        image: '/talks/2026-from-interview-to-agent/agent-legwork.png'
      }
    ]
  : [
      {
        label: '分享 / 工程边界',
        title: 'Agent 工程真正包含什么',
        image: '/talks/2026-from-interview-to-agent/engineering-scope.png'
      },
      {
        label: '方法 / 成长回路',
        title: '从面试到生产的可重复成长路径',
        image: '/talks/2026-from-interview-to-agent/growth-loop.png'
      },
      {
        label: '实践 / Agent 苦工',
        title: '可靠 Agent 背后那些不光鲜的工作',
        image: '/talks/2026-from-interview-to-agent/agent-legwork.png'
      }
    ]

const totalStars = repos.reduce((sum, repo) => sum + repo.stars, 0)
---

<IntroOverlay locale={locale} />

<visual-home class='visual-home'>
  <nav class='home-nav' aria-label={isEn ? 'Primary navigation' : '主导航'}>
    <a class='home-nav__brand' href={prefix || '/'}>Joye Huang</a>
    <div class='home-nav__links'>
      <a href='#work'>{copy.work}</a>
      <a href='#evidence'>{copy.about}</a>
      <a href='#journal'>{copy.journal}</a>
    </div>
    <div class='home-nav__end'>
      <a href={isEn ? '/' : '/en'}>{isEn ? '中文' : 'EN'}</a>
      <a href={`${prefix}/contact`}>{copy.contact} ↗</a>
    </div>
  </nav>

  <main>
    <section class='hero' id='top' data-hero>
      <div class='hero__copy'>
        <div class='hero__eyebrow'><span>{copy.role}</span><span>{copy.location}</span></div>
        <h1>Joye<br />Huang</h1>
        <p>{copy.intro}</p>
        <a href='#work'><i></i>{copy.explore}</a>
      </div>

      <div class='hero__world' aria-label={isEn ? 'Selected product interfaces' : '代表产品界面'}>
        <div class='hero__stage' data-hero-stage>
          <figure class='hero-screen hero-screen--playyy'>
            <img
              src='/images/landing/playyy-ui.png'
              alt='Playyy product interface'
              loading='eager'
            />
            <figcaption>01 / PLAYYY</figcaption>
          </figure>
          <figure class='hero-screen hero-screen--atypica'>
            <img
              src='/images/landing/atypica-site.png'
              alt='atypica public product page'
              loading='eager'
            />
            <figcaption>02 / ATYPICA</figcaption>
          </figure>
          <figure class='hero-screen hero-screen--aixcut'>
            <img
              src='/images/landing/aixcut-site.png'
              alt='AIXCut public product page'
              loading='eager'
            />
            <figcaption>03 / AIXCUT</figcaption>
          </figure>
        </div>
      </div>

      <div class='hero__portrait'>
        <Image
          src={avatar}
          alt={isEn ? 'Portrait of Joye' : 'Joye 的头像'}
          data-intro-target='avatar'
        />
      </div>
      <span class='hero__index'>PORTFOLIO / 2026</span>
    </section>

    <section class='work-intro' id='work'>
      <span>01—03 / PRODUCTS</span>
      <h2>{copy.selected}</h2>
      <p>{copy.selectedBody}</p>
    </section>

    {
      projects.map((project, index) => (
        <section
          class:list={['case-study', `case-study--${project.key}`]}
          style={`--case-accent:${project.accent}`}
          data-case
        >
          <div class='case-study__sticky'>
            <div class='case-study__copy' data-case-copy>
              <span>{project.index} / 2025—26</span>
              <h2>{project.title}</h2>
              <strong>{project.role}</strong>
              <p>{project.body}</p>
              <a href={project.href} target='_blank' rel='noopener noreferrer'>
                {copy.visit} ↗
              </a>
            </div>

            <div class='case-study__scene' data-case-scene>
              <div class='case-study__halo' aria-hidden='true' />
              <figure class='product-plane' data-case-screen>
                <div class='product-plane__bar'>
                  <span>
                    <>
                      <i />
                      <i />
                      <i />
                    </>
                  </span>
                  <em>{project.domain}</em>
                  <b>LIVE / 0{index + 1}</b>
                </div>
                <img
                  src={project.image}
                  alt={project.alt}
                  loading={index === 0 ? 'eager' : 'lazy'}
                />
              </figure>
              <aside class='case-study__depth' data-depth-card>
                <span>SYSTEM LAYER</span>
                <strong>{project.role}</strong>
                <i>{project.index}</i>
              </aside>
            </div>
          </div>
        </section>
      ))
    }

    <section class='evidence' id='evidence' data-evidence>
      <div class='evidence__sticky'>
        <header>
          <span>04 / PUBLIC PRACTICE</span>
          <h2>{copy.evidence}</h2>
          <p>{copy.evidenceBody}</p>
          <a href={`${prefix}/talks`}>{copy.evidenceCta} ↗</a>
        </header>
        <div class='evidence__scene' data-evidence-scene>
          {
            evidence.map((item, index) => (
              <figure class={`evidence-card evidence-card--${index + 1}`} data-evidence-card>
                <img src={item.image} alt={item.title} loading='lazy' />
                <figcaption>
                  <>
                    <span>{item.label}</span>
                    <strong>{item.title}</strong>
                  </>
                </figcaption>
              </figure>
            ))
          }
        </div>
      </div>
    </section>

    <section class='journal' id='journal'>
      <header class='journal__intro' data-reveal>
        <span>05 / JOURNAL</span>
        <h2>{copy.journalTitle}</h2>
        <p>{copy.journalBody}</p>
      </header>
      <div class='journal__columns'>
        <section class='journal__column' data-reveal>
          <div>
            <span>{copy.essays}</span><strong>{posts.length.toString().padStart(2, '0')}</strong>
          </div>
          {
            posts.slice(0, 4).map((post) => (
              <a href={`${prefix}${post.href}`}>
                <>
                  <span>{post.meta}</span>
                  <h3>{post.title}</h3>
                  <i>↗</i>
                </>
              </a>
            ))
          }
        </section>
        <section class='journal__column' data-reveal>
          <div>
            <span>{copy.notes}</span><strong>{notes.length.toString().padStart(2, '0')}</strong>
          </div>
          {
            notes.slice(0, 4).map((note) => (
              <a href={`${prefix}${note.href}`}>
                <>
                  <span>{note.meta}</span>
                  <h3>{note.title}</h3>
                  <i>↗</i>
                </>
              </a>
            ))
          }
        </section>
      </div>
    </section>

    <section class='source' data-reveal>
      <div class='source__visual'>
        <img
          src='/talks/2026-from-interview-to-agent/agent-legwork-crop.png'
          alt='Agent engineering work map'
          loading='lazy'
        />
      </div>
      <div class='source__copy'>
        <span>06 / SOURCE</span>
        <h2>{copy.openSource}</h2>
        <p>{copy.sourceBody}</p>
        <strong>{totalStars}+ GitHub Stars</strong>
        <ul>
          {
            repos.slice(0, 3).map((repo) => (
              <li>
                <a
                  href={`https://github.com/joyehuang/${repo.name}`}
                  target='_blank'
                  rel='noopener noreferrer'
                >
                  <>
                    <span>{repo.name}</span>
                    <i>★ {repo.stars} ↗</i>
                  </>
                </a>
              </li>
            ))
          }
        </ul>
      </div>
    </section>

    <section class='finale' data-finale>
      <div class='finale__copy'>
        <span>07 / START WITH THE MESS</span>
        <h2>{copy.finale}</h2>
        <p>{copy.finaleBody}</p>
        <div>
          <a href={`${prefix}/contact`}>{copy.talk} ↗</a>
          <button type='button' data-replay-intro>{copy.replay} ↻</button>
        </div>
      </div>

      <div
        class='problem-portal'
        data-problem-portal
        aria-label={isEn ? 'Brief structure' : 'Brief 结构'}
      >
        <div class='problem-card problem-card--context' data-problem-layer>
          <span>01 / {copy.context}</span><p>{copy.contextValue}</p>
        </div>
        <div class='problem-card problem-card--constraint' data-problem-layer>
          <span>02 / {copy.constraint}</span><p>{copy.constraintValue}</p>
        </div>
        <div class='problem-card problem-card--outcome' data-problem-layer>
          <span>03 / {copy.outcome}</span><p>{copy.outcomeValue}</p>
        </div>
        <div class='problem-card problem-card--ready' data-solution>
          <i></i><strong>{copy.ready}</strong><span>JOYE / 2026</span>
        </div>
      </div>

      <footer>
        <span>© {new Date().getFullYear()} JOYE HUANG</span>
        <span>{copy.copyright}</span>
        <a href='#top'>TOP ↑</a>
      </footer>
    </section>
  </main>

  <div id='home-jojo' class='home-jojo'><JoJo client:load size='md' quipPool='idle' /></div>
</visual-home>

<script>
  import { gsap } from 'gsap'
  import { ScrollTrigger } from 'gsap/ScrollTrigger'

  gsap.registerPlugin(ScrollTrigger)

  class VisualHome extends HTMLElement {
    private context?: gsap.Context
    private cleanups: Array<() => void> = []

    connectedCallback() {
      if (this.context) return

      const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches
      const compact = window.matchMedia('(max-width: 600px)').matches

      this.context = gsap.context(() => {
        if (reduced) return

        const hero = this.querySelector<HTMLElement>('[data-hero]')
        const heroStage = this.querySelector<HTMLElement>('[data-hero-stage]')
        const heroScreens = this.querySelectorAll<HTMLElement>('.hero-screen')

        gsap
          .timeline({ defaults: { ease: 'power3.out' } })
          .from('.hero__eyebrow, .hero__copy h1, .hero__copy > p, .hero__copy > a', {
            opacity: 0,
            y: 28,
            duration: 0.9,
            stagger: 0.08
          })
          .from(
            heroScreens,
            {
              opacity: 0,
              z: -700,
              rotationY: 28,
              duration: 1.35,
              stagger: 0.12
            },
            0.12
          )

        if (hero && heroStage) {
          gsap.to(heroStage, {
            rotationX: -12,
            rotationY: 8,
            z: -320,
            yPercent: 20,
            ease: 'none',
            scrollTrigger: { trigger: hero, start: 'top top', end: 'bottom top', scrub: 1.1 }
          })

          const toX = gsap.quickTo(heroStage, 'rotationY', { duration: 0.8, ease: 'power3.out' })
          const toY = gsap.quickTo(heroStage, 'rotationX', { duration: 0.8, ease: 'power3.out' })
          const move = (event: PointerEvent) => {
            const rect = hero.getBoundingClientRect()
            if (rect.bottom < 0) return
            toX(((event.clientX - rect.width / 2) / rect.width) * 10)
            toY(((event.clientY - rect.height / 2) / rect.height) * -7)
          }
          hero.addEventListener('pointermove', move)
          this.cleanups.push(() => hero.removeEventListener('pointermove', move))
        }

        this.querySelectorAll<HTMLElement>('[data-case]').forEach((section, index) => {
          const screen = section.querySelector<HTMLElement>('[data-case-screen]')
          const copy = section.querySelector<HTMLElement>('[data-case-copy]')
          const depth = section.querySelector<HTMLElement>('[data-depth-card]')
          if (!screen || !copy || !depth) return

          if (compact) {
            gsap.from(screen, {
              opacity: 0,
              y: 32,
              scale: 0.94,
              rotationX: 8,
              duration: 0.85,
              ease: 'power3.out',
              scrollTrigger: { trigger: section, start: 'top 72%', once: true }
            })
            gsap.from(copy, {
              opacity: 0,
              y: 24,
              duration: 0.75,
              ease: 'power3.out',
              scrollTrigger: { trigger: section, start: 'top 78%', once: true }
            })
            return
          }

          const direction = index % 2 === 0 ? 1 : -1
          const timeline = gsap.timeline({
            scrollTrigger: {
              trigger: section,
              start: 'top top',
              end: 'bottom bottom',
              scrub: 1.15
            }
          })

          timeline
            .fromTo(
              screen,
              {
                opacity: 0.12,
                rotationY: 30 * direction,
                rotationX: 13,
                z: -580,
                xPercent: 18 * direction
              },
              {
                opacity: 1,
                rotationY: 0,
                rotationX: 0,
                z: 0,
                xPercent: 0,
                duration: 0.46,
                ease: 'power2.out'
              }
            )
            .fromTo(
              copy,
              { opacity: 0, xPercent: -20 * direction, z: -180 },
              { opacity: 1, xPercent: 0, z: 0, duration: 0.32, ease: 'power2.out' },
              0.08
            )
            .fromTo(
              depth,
              { opacity: 0, rotationY: -36 * direction, z: -480 },
              { opacity: 1, rotationY: -9 * direction, z: -90, duration: 0.36 },
              0.15
            )
            .to(
              screen,
              {
                opacity: 0.38,
                rotationY: -14 * direction,
                rotationX: -8,
                z: 300,
                xPercent: -8 * direction,
                duration: 0.18
              },
              0.88
            )
            .to(copy, { opacity: 0.12, z: 150, duration: 0.16 }, 0.9)
        })

        const evidenceSection = this.querySelector<HTMLElement>('[data-evidence]')
        const evidenceCards = gsap.utils.toArray<HTMLElement>('[data-evidence-card]', this)
        if (evidenceSection && evidenceCards.length && !compact) {
          const evidenceTimeline = gsap.timeline({
            scrollTrigger: {
              trigger: evidenceSection,
              start: 'top top',
              end: 'bottom bottom',
              scrub: 1
            }
          })
          evidenceTimeline
            .fromTo(
              evidenceCards,
              { opacity: 0, z: -720, rotationY: 34, rotationX: 12 },
              {
                opacity: 1,
                z: (i) => i * 65,
                rotationY: (i) => (i - 1) * -8,
                rotationX: 0,
                stagger: 0.08,
                duration: 0.5
              }
            )
            .to(
              evidenceCards,
              {
                xPercent: (i) => (i - 1) * 54,
                rotationY: (i) => (i - 1) * -16,
                z: (i) => 180 - Math.abs(i - 1) * 120,
                duration: 0.48
              },
              0.48
            )
        }

        gsap.utils.toArray<HTMLElement>('[data-reveal]', this).forEach((element) => {
          gsap.from(element, {
            opacity: 0,
            y: 48,
            duration: 0.9,
            ease: 'power3.out',
            scrollTrigger: { trigger: element, start: 'top 86%', once: true }
          })
        })

        const finale = this.querySelector<HTMLElement>('[data-finale]')
        const problemLayers = gsap.utils.toArray<HTMLElement>('[data-problem-layer]', this)
        const solution = this.querySelector<HTMLElement>('[data-solution]')
        if (finale && solution && problemLayers.length && compact) {
          gsap.from(problemLayers, {
            opacity: 0,
            y: 34,
            rotationX: 8,
            duration: 0.75,
            stagger: 0.08,
            ease: 'power3.out',
            scrollTrigger: { trigger: finale, start: 'top 70%', once: true }
          })
          gsap.from(solution, {
            opacity: 0,
            scale: 0.86,
            duration: 0.7,
            ease: 'power3.out',
            scrollTrigger: { trigger: finale, start: 'top 62%', once: true }
          })
        } else if (finale && solution && problemLayers.length) {
          const problemTimeline = gsap.timeline({
            scrollTrigger: { trigger: finale, start: 'top 72%', end: 'center center', scrub: 0.8 }
          })
          problemTimeline
            .fromTo(
              problemLayers,
              {
                opacity: 0,
                z: -520,
                rotationY: (i) => (i - 1) * 28,
                xPercent: (i) => (i - 1) * 44
              },
              {
                opacity: 1,
                z: (i) => i * 55,
                rotationY: (i) => (i - 1) * 8,
                xPercent: (i) => (i - 1) * 13,
                stagger: 0.06,
                duration: 0.58
              }
            )
            .fromTo(
              solution,
              { opacity: 0, scale: 0.62, z: -260 },
              { opacity: 1, scale: 1, z: 180, duration: 0.32 },
              0.52
            )
        }
      }, this)

      const replay = this.querySelector<HTMLButtonElement>('[data-replay-intro]')
      const playIntro = () => window.__joyeIntro?.play('jojo')
      replay?.addEventListener('click', playIntro)
      this.cleanups.push(() => replay?.removeEventListener('click', playIntro))

      requestAnimationFrame(() => ScrollTrigger.refresh())
    }

    disconnectedCallback() {
      this.cleanups.forEach((cleanup) => cleanup())
      this.cleanups = []
      this.context?.revert()
      this.context = undefined
    }
  }

  if (!customElements.get('visual-home')) customElements.define('visual-home', VisualHome)

  declare global {
    interface Window {
      __joyeIntro?: { play(variant: 'focus' | 'line' | 'jojo'): void }
    }
  }
</script>

<style>
  .visual-home {
    --home-bg: hsl(var(--background));
    --home-fg: hsl(var(--foreground));
    --home-card: hsl(var(--card));
    --home-muted: hsl(var(--muted-foreground));
    --home-border: hsl(var(--border));
    --home-accent: hsl(var(--primary));
    display: block;
    overflow: clip;
    color: var(--home-fg);
    background: var(--home-bg);
  }

  .visual-home * {
    box-sizing: border-box;
  }

  .visual-home a {
    color: inherit;
    text-decoration: none;
  }

  .home-nav {
    position: fixed;
    z-index: 30;
    top: 0;
    left: 0;
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    width: 100%;
    padding: 1.15rem clamp(1rem, 3vw, 3rem);
    border-bottom: 1px solid color-mix(in srgb, var(--home-border), transparent 25%);
    background: color-mix(in srgb, var(--home-bg), transparent 18%);
    backdrop-filter: blur(18px);
    font-family: ui-monospace, monospace;
    font-size: 0.64rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .home-nav__brand {
    font-family: inherit;
    font-weight: 700;
  }

  .home-nav__links,
  .home-nav__end {
    display: flex;
    gap: 1.8rem;
  }

  .home-nav__end {
    justify-self: end;
  }

  .home-nav a {
    transition: color 0.25s ease;
  }

  .home-nav a:hover {
    color: var(--home-accent);
  }

  .hero {
    position: relative;
    min-height: 100svh;
    overflow: hidden;
    padding: 8.5rem clamp(1rem, 4vw, 4rem) 4rem;
    background:
      linear-gradient(color-mix(in srgb, var(--home-border), transparent 72%) 1px, transparent 1px),
      linear-gradient(
        90deg,
        color-mix(in srgb, var(--home-border), transparent 72%) 1px,
        transparent 1px
      ),
      radial-gradient(
        circle at 72% 42%,
        color-mix(in srgb, var(--home-accent), transparent 73%),
        transparent 38%
      );
    background-size:
      4.5rem 4.5rem,
      4.5rem 4.5rem,
      auto;
  }

  .hero__copy {
    position: relative;
    z-index: 4;
    width: min(43rem, 56vw);
  }

  .hero__eyebrow {
    display: flex;
    justify-content: space-between;
    gap: 2rem;
    color: var(--home-muted);
    font-family: ui-monospace, monospace;
    font-size: 0.62rem;
    letter-spacing: 0.11em;
    text-transform: uppercase;
  }

  .hero h1 {
    margin: clamp(3rem, 7vh, 6rem) 0 1.8rem;
    font-size: clamp(5.4rem, 11vw, 10.5rem);
    font-weight: 520;
    letter-spacing: -0.085em;
    line-height: 0.72;
  }

  .hero__copy > p {
    width: min(31rem, 86%);
    margin: 0 0 2.2rem;
    color: var(--home-muted);
    font-size: clamp(1rem, 1.45vw, 1.3rem);
    line-height: 1.48;
  }

  .hero__copy > a {
    display: inline-flex;
    gap: 0.8rem;
    align-items: center;
    padding: 0.85rem 1rem;
    border: 1px solid var(--home-border);
    border-radius: 999px;
    font-family: ui-monospace, monospace;
    font-size: 0.62rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .hero__copy > a i {
    width: 0.45rem;
    aspect-ratio: 1;
    border-radius: 50%;
    background: var(--home-accent);
    box-shadow: 0 0 1.2rem var(--home-accent);
  }

  .hero__world {
    position: absolute;
    z-index: 2;
    inset: 9rem -5vw 2rem 38vw;
    perspective: 1500px;
  }

  .hero__stage {
    position: relative;
    width: 100%;
    height: 100%;
    transform-style: preserve-3d;
    will-change: transform;
  }

  .hero-screen {
    position: absolute;
    width: min(53vw, 58rem);
    margin: 0;
    overflow: hidden;
    border: 1px solid color-mix(in srgb, var(--home-border), white 15%);
    border-radius: 0.7rem;
    background: #090b0e;
    box-shadow: 0 2.5rem 7rem color-mix(in srgb, var(--home-fg), transparent 76%);
    transform-style: preserve-3d;
    backface-visibility: hidden;
  }

  .hero-screen img {
    display: block;
    width: 100%;
    aspect-ratio: 16 / 9;
    object-fit: cover;
    object-position: top;
  }

  .hero-screen figcaption {
    position: absolute;
    right: 0.75rem;
    bottom: 0.65rem;
    padding: 0.38rem 0.55rem;
    color: white;
    background: rgb(0 0 0 / 70%);
    font-family: ui-monospace, monospace;
    font-size: 0.54rem;
    letter-spacing: 0.11em;
  }

  .hero-screen--playyy {
    top: 7%;
    left: 10%;
    transform: translateZ(40px) rotateY(-13deg) rotateX(4deg);
  }

  .hero-screen--atypica {
    top: 28%;
    left: 25%;
    transform: translateZ(150px) rotateY(-7deg) rotateX(2deg);
  }

  .hero-screen--aixcut {
    top: 49%;
    left: 4%;
    transform: translateZ(280px) rotateY(8deg) rotateX(-3deg);
  }

  .hero__portrait {
    position: absolute;
    z-index: 5;
    right: 2rem;
    bottom: 2rem;
    width: 3.4rem;
    overflow: hidden;
    border: 1px solid var(--home-border);
    border-radius: 50%;
  }

  .hero__portrait :global(img) {
    display: block;
    width: 100%;
  }

  .hero__index {
    position: absolute;
    bottom: 2.8rem;
    left: clamp(1rem, 4vw, 4rem);
    font-family: ui-monospace, monospace;
    font-size: 0.58rem;
    letter-spacing: 0.12em;
  }

  .work-intro {
    display: grid;
    grid-template-columns: 0.55fr 1.45fr 0.9fr;
    gap: clamp(1.5rem, 4vw, 5rem);
    align-items: end;
    padding: clamp(7rem, 12vw, 12rem) clamp(1rem, 4vw, 4rem);
    border-top: 1px solid var(--home-border);
  }

  .work-intro > span,
  .case-study__copy > span,
  .evidence header > span,
  .journal__intro > span,
  .source__copy > span,
  .finale__copy > span {
    color: var(--home-muted);
    font-family: ui-monospace, monospace;
    font-size: 0.6rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .work-intro h2 {
    max-width: 52rem;
    margin: 0;
    font-size: clamp(3.2rem, 6.8vw, 7.2rem);
    font-weight: 520;
    letter-spacing: -0.065em;
    line-height: 0.92;
  }

  .work-intro p {
    max-width: 28rem;
    margin: 0;
    color: var(--home-muted);
    line-height: 1.5;
  }

  .case-study {
    --case-accent: var(--home-accent);
    position: relative;
    height: 190vh;
    border-top: 1px solid var(--home-border);
    background:
      radial-gradient(
        circle at 70% 50%,
        color-mix(in srgb, var(--case-accent), transparent 82%),
        transparent 35%
      ),
      var(--home-bg);
  }

  .case-study__sticky {
    position: sticky;
    top: 0;
    height: 100svh;
    overflow: hidden;
    perspective: 1600px;
  }

  .case-study__copy {
    position: absolute;
    z-index: 4;
    top: 50%;
    left: clamp(1rem, 4vw, 4rem);
    width: min(30rem, 31vw);
    transform: translateY(-50%);
    transform-style: preserve-3d;
  }

  .case-study__copy h2 {
    margin: 1.2rem 0 1.1rem;
    font-size: clamp(4.5rem, 8.5vw, 9rem);
    font-weight: 520;
    letter-spacing: -0.08em;
    line-height: 0.78;
  }

  .case-study__copy strong {
    display: block;
    margin-bottom: 1rem;
    color: var(--case-accent);
    font-family: ui-monospace, monospace;
    font-size: 0.68rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .case-study__copy p {
    max-width: 27rem;
    margin: 0 0 2rem;
    color: var(--home-muted);
    font-size: clamp(0.95rem, 1.3vw, 1.15rem);
    line-height: 1.55;
  }

  .case-study__copy a {
    display: inline-block;
    padding: 0.75rem 0;
    border-bottom: 1px solid var(--case-accent);
    font-family: ui-monospace, monospace;
    font-size: 0.62rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .case-study__scene {
    position: absolute;
    z-index: 2;
    inset: 7rem 2vw 4rem 35vw;
    display: grid;
    place-items: center;
    perspective: 1500px;
    transform-style: preserve-3d;
  }

  .case-study__halo {
    position: absolute;
    width: min(48vw, 48rem);
    aspect-ratio: 1;
    border-radius: 50%;
    background: radial-gradient(
      circle,
      color-mix(in srgb, var(--case-accent), transparent 64%),
      transparent 69%
    );
    filter: blur(30px);
  }

  .product-plane {
    position: relative;
    z-index: 2;
    width: min(62vw, 70rem);
    margin: 0;
    overflow: hidden;
    border: 1px solid color-mix(in srgb, var(--case-accent), var(--home-border) 65%);
    border-radius: 0.85rem;
    background: #08090b;
    box-shadow:
      0 4rem 10rem color-mix(in srgb, var(--home-fg), transparent 78%),
      0 0 0 0.5rem color-mix(in srgb, var(--home-card), transparent 35%);
    transform-style: preserve-3d;
    will-change: transform, opacity;
  }

  .product-plane__bar {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    height: 2.7rem;
    padding: 0 0.9rem;
    color: rgb(255 255 255 / 60%);
    border-bottom: 1px solid rgb(255 255 255 / 12%);
    font-family: ui-monospace, monospace;
    font-size: 0.54rem;
    letter-spacing: 0.08em;
  }

  .product-plane__bar > span {
    display: flex;
    gap: 0.35rem;
  }

  .product-plane__bar i {
    width: 0.42rem;
    aspect-ratio: 1;
    border-radius: 50%;
    background: rgb(255 255 255 / 30%);
  }

  .product-plane__bar em {
    font-style: normal;
  }

  .product-plane__bar b {
    justify-self: end;
    color: var(--case-accent);
    font-weight: 500;
  }

  .product-plane > img {
    display: block;
    width: 100%;
    aspect-ratio: 16 / 9;
    object-fit: cover;
    object-position: top;
  }

  .case-study__depth {
    position: absolute;
    z-index: 1;
    right: 2%;
    bottom: 2%;
    display: grid;
    width: min(20rem, 26vw);
    min-height: 9rem;
    padding: 1.2rem;
    color: var(--home-fg);
    border: 1px solid color-mix(in srgb, var(--case-accent), transparent 50%);
    background: color-mix(in srgb, var(--home-card), transparent 9%);
    box-shadow: 0 2rem 5rem color-mix(in srgb, var(--home-fg), transparent 85%);
    transform-style: preserve-3d;
  }

  .case-study__depth span,
  .case-study__depth strong {
    font-family: ui-monospace, monospace;
    font-size: 0.58rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .case-study__depth strong {
    max-width: 13rem;
    color: var(--home-muted);
    font-weight: 500;
    line-height: 1.45;
  }

  .case-study__depth i {
    position: absolute;
    right: 0.8rem;
    bottom: 0.35rem;
    color: color-mix(in srgb, var(--case-accent), transparent 25%);
    font-size: 4.5rem;
    font-style: normal;
    line-height: 1;
  }

  .evidence {
    position: relative;
    height: 220vh;
    border-top: 1px solid var(--home-border);
    background:
      radial-gradient(
        circle at 50% 58%,
        color-mix(in srgb, var(--home-accent), transparent 78%),
        transparent 32%
      ),
      var(--home-bg);
  }

  .evidence__sticky {
    position: sticky;
    top: 0;
    height: 100svh;
    overflow: hidden;
    padding: 7rem clamp(1rem, 4vw, 4rem) 3rem;
  }

  .evidence header {
    position: relative;
    z-index: 5;
    width: min(32rem, 33vw);
  }

  .evidence h2,
  .journal h2,
  .source h2,
  .finale h2 {
    margin: 1rem 0 1.2rem;
    font-size: clamp(3.4rem, 7vw, 7.5rem);
    font-weight: 520;
    letter-spacing: -0.07em;
    line-height: 0.9;
  }

  .evidence header p {
    color: var(--home-muted);
    line-height: 1.55;
  }

  .evidence header a {
    display: inline-block;
    margin-top: 1rem;
    padding-bottom: 0.5rem;
    border-bottom: 1px solid var(--home-accent);
    font-family: ui-monospace, monospace;
    font-size: 0.62rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .evidence__scene {
    position: absolute;
    inset: 7rem 3vw 3rem 37vw;
    perspective: 1600px;
    transform-style: preserve-3d;
  }

  .evidence-card {
    position: absolute;
    top: 50%;
    left: 50%;
    width: min(47vw, 51rem);
    margin: 0;
    overflow: hidden;
    border: 1px solid var(--home-border);
    border-radius: 0.75rem;
    background: var(--home-card);
    box-shadow: 0 3rem 8rem color-mix(in srgb, var(--home-fg), transparent 78%);
    transform: translate(-50%, -50%);
    transform-style: preserve-3d;
    will-change: transform, opacity;
  }

  .evidence-card img {
    display: block;
    width: 100%;
    aspect-ratio: 16 / 9;
    object-fit: cover;
  }

  .evidence-card figcaption {
    display: grid;
    grid-template-columns: 0.7fr 1.3fr;
    gap: 1rem;
    padding: 1rem;
    border-top: 1px solid var(--home-border);
  }

  .evidence-card figcaption span {
    color: var(--home-muted);
    font-family: ui-monospace, monospace;
    font-size: 0.56rem;
    letter-spacing: 0.09em;
    text-transform: uppercase;
  }

  .evidence-card figcaption strong {
    font-size: 0.92rem;
    font-weight: 520;
  }

  .journal {
    padding: clamp(7rem, 12vw, 12rem) clamp(1rem, 4vw, 4rem);
    border-top: 1px solid var(--home-border);
  }

  .journal__intro {
    display: grid;
    grid-template-columns: 0.55fr 1.45fr 0.9fr;
    gap: clamp(1.5rem, 4vw, 5rem);
    align-items: end;
    margin-bottom: 7rem;
  }

  .journal__intro h2 {
    margin: 0;
  }

  .journal__intro p {
    margin: 0;
    color: var(--home-muted);
    line-height: 1.5;
  }

  .journal__columns {
    display: grid;
    grid-template-columns: 1fr 1fr;
    border-top: 1px solid var(--home-border);
  }

  .journal__column:first-child {
    padding-right: 3vw;
    border-right: 1px solid var(--home-border);
  }

  .journal__column:last-child {
    padding-left: 3vw;
  }

  .journal__column > div {
    display: flex;
    justify-content: space-between;
    padding: 1.1rem 0 2.2rem;
    color: var(--home-muted);
    font-family: ui-monospace, monospace;
    font-size: 0.6rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .journal__column > a {
    display: grid;
    grid-template-columns: 6.5rem 1fr auto;
    gap: 1rem;
    align-items: start;
    padding: 1.35rem 0;
    border-top: 1px solid var(--home-border);
  }

  .journal__column > a span,
  .journal__column > a i {
    color: var(--home-muted);
    font-family: ui-monospace, monospace;
    font-size: 0.56rem;
    font-style: normal;
  }

  .journal__column h3 {
    margin: 0;
    font-size: clamp(1rem, 1.35vw, 1.25rem);
    font-weight: 500;
    line-height: 1.28;
  }

  .source {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    min-height: 90svh;
    border-top: 1px solid var(--home-border);
  }

  .source__visual {
    display: grid;
    place-items: center;
    min-height: 55vh;
    overflow: hidden;
    padding: clamp(2rem, 5vw, 6rem);
    background: color-mix(in srgb, var(--home-accent), var(--home-bg) 88%);
  }

  .source__visual img {
    display: block;
    width: min(100%, 54rem);
    border: 1px solid var(--home-border);
    border-radius: 0.65rem;
    box-shadow: 0 2rem 6rem color-mix(in srgb, var(--home-fg), transparent 82%);
  }

  .source__copy {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: clamp(4rem, 7vw, 8rem);
    border-left: 1px solid var(--home-border);
  }

  .source__copy p {
    color: var(--home-muted);
  }

  .source__copy > strong {
    margin: 2rem 0 3rem;
    color: var(--home-accent);
    font-family: ui-monospace, monospace;
    font-size: 0.72rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .source ul {
    margin: 0;
    padding: 0;
    list-style: none;
    border-top: 1px solid var(--home-border);
  }

  .source li a {
    display: flex;
    justify-content: space-between;
    padding: 1rem 0;
    border-bottom: 1px solid var(--home-border);
  }

  .source li i {
    color: var(--home-muted);
    font-family: ui-monospace, monospace;
    font-size: 0.58rem;
    font-style: normal;
  }

  .finale {
    position: relative;
    display: grid;
    grid-template-columns: 0.8fr 1.2fr;
    min-height: 105svh;
    overflow: hidden;
    padding: 8rem clamp(1rem, 4vw, 4rem) 6rem;
    border-top: 1px solid var(--home-border);
    background:
      linear-gradient(color-mix(in srgb, var(--home-border), transparent 78%) 1px, transparent 1px),
      linear-gradient(
        90deg,
        color-mix(in srgb, var(--home-border), transparent 78%) 1px,
        transparent 1px
      ),
      radial-gradient(
        circle at 70% 50%,
        color-mix(in srgb, var(--home-accent), transparent 70%),
        transparent 34%
      ),
      var(--home-bg);
    background-size:
      4rem 4rem,
      4rem 4rem,
      auto,
      auto;
  }

  .finale__copy {
    position: relative;
    z-index: 4;
    align-self: center;
    width: min(35rem, 90%);
  }

  .finale__copy p {
    max-width: 31rem;
    color: var(--home-muted);
    font-size: 1.05rem;
    line-height: 1.55;
  }

  .finale__copy > div {
    display: flex;
    gap: 1rem;
    margin-top: 2rem;
  }

  .finale__copy a,
  .finale__copy button {
    padding: 0.9rem 1.1rem;
    color: var(--home-fg);
    border: 1px solid var(--home-border);
    border-radius: 999px;
    background: color-mix(in srgb, var(--home-bg), transparent 12%);
    font-family: ui-monospace, monospace;
    font-size: 0.6rem;
    letter-spacing: 0.09em;
    text-transform: uppercase;
    cursor: pointer;
  }

  .finale__copy a {
    color: var(--home-bg);
    border-color: var(--home-accent);
    background: var(--home-accent);
  }

  .problem-portal {
    position: relative;
    min-height: 38rem;
    perspective: 1500px;
    transform-style: preserve-3d;
  }

  .problem-card {
    position: absolute;
    top: 50%;
    left: 50%;
    width: min(28rem, 36vw);
    min-height: 11rem;
    padding: 1.3rem;
    border: 1px solid var(--home-border);
    background: color-mix(in srgb, var(--home-card), transparent 4%);
    box-shadow: 0 2rem 5rem color-mix(in srgb, var(--home-fg), transparent 85%);
    transform: translate(-50%, -50%);
    transform-style: preserve-3d;
  }

  .problem-card > span,
  .problem-card--ready strong {
    font-family: ui-monospace, monospace;
    font-size: 0.6rem;
    letter-spacing: 0.11em;
    text-transform: uppercase;
  }

  .problem-card p {
    margin: 3.3rem 0 0;
    font-size: 1.15rem;
  }

  .problem-card--context {
    margin: -10rem 0 0 -11rem;
  }

  .problem-card--constraint {
    margin: 0 0 0 9rem;
  }

  .problem-card--outcome {
    margin: 9rem 0 0 -6rem;
  }

  .problem-card--ready {
    z-index: 5;
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: 0.8rem;
    align-items: center;
    min-height: auto;
    color: var(--home-bg);
    border-color: var(--home-accent);
    background: var(--home-accent);
  }

  .problem-card--ready i {
    width: 0.5rem;
    aspect-ratio: 1;
    border-radius: 50%;
    background: currentColor;
  }

  .problem-card--ready span {
    font-size: 0.52rem;
  }

  .finale footer {
    position: absolute;
    right: clamp(1rem, 4vw, 4rem);
    bottom: 1.5rem;
    left: clamp(1rem, 4vw, 4rem);
    display: flex;
    justify-content: space-between;
    color: var(--home-muted);
    font-family: ui-monospace, monospace;
    font-size: 0.56rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .home-jojo {
    position: fixed;
    z-index: 20;
    right: 1rem;
    bottom: 1rem;
  }

  @media (max-width: 900px) {
    .home-nav {
      grid-template-columns: 1fr auto;
    }
    .home-nav__links {
      display: none;
    }
    .hero {
      min-height: 110svh;
      padding-top: 7rem;
    }
    .hero__copy {
      width: 90%;
    }
    .hero__world {
      inset: 46% -12vw 3rem 8vw;
    }
    .hero-screen {
      width: min(76vw, 44rem);
    }
    .work-intro,
    .journal__intro {
      grid-template-columns: 1fr;
    }
    .case-study__copy {
      top: 18%;
      width: 84vw;
      transform: none;
    }
    .case-study__copy h2 {
      margin: 0.8rem 0;
      font-size: clamp(4rem, 12vw, 7rem);
    }
    .case-study__copy p {
      max-width: 34rem;
    }
    .case-study__scene {
      inset: 39% 3vw 4rem 3vw;
    }
    .product-plane {
      width: min(88vw, 60rem);
    }
    .case-study__depth {
      width: min(18rem, 40vw);
    }
    .evidence header {
      width: min(38rem, 86vw);
    }
    .evidence__scene {
      inset: 42% 4vw 2rem;
    }
    .evidence-card {
      width: min(66vw, 44rem);
    }
    .journal__columns,
    .source,
    .finale {
      grid-template-columns: 1fr;
    }
    .journal__column:first-child {
      padding-right: 0;
      border-right: 0;
    }
    .journal__column:last-child {
      padding-left: 0;
    }
    .source__copy {
      border-top: 1px solid var(--home-border);
      border-left: 0;
    }
    .finale__copy {
      align-self: start;
    }
    .problem-portal {
      min-height: 35rem;
    }
    .problem-card {
      width: min(27rem, 52vw);
    }
  }

  @media (max-width: 600px) {
    .home-nav__end a:first-child {
      display: none;
    }
    .hero {
      min-height: 100svh;
      padding: 6.7rem 1rem 2rem;
    }
    .hero__eyebrow span:last-child {
      display: none;
    }
    .hero h1 {
      margin-top: 2.4rem;
      font-size: clamp(4.8rem, 25vw, 7rem);
    }
    .hero__world {
      inset: 52% -24vw 2rem 2vw;
    }
    .hero-screen {
      width: 84vw;
    }
    .hero-screen--playyy {
      top: 2%;
      left: 7%;
    }
    .hero-screen--atypica {
      top: 24%;
      left: 18%;
    }
    .hero-screen--aixcut {
      top: 46%;
      left: 0;
    }
    .hero__portrait,
    .hero__index {
      display: none;
    }
    .work-intro {
      padding: 6rem 1rem;
    }
    .work-intro h2 {
      font-size: 3.6rem;
    }
    .case-study {
      height: auto;
      min-height: 100svh;
    }
    .case-study__sticky {
      position: relative;
      display: flex;
      flex-direction: column;
      height: auto;
      min-height: 100svh;
      padding: 6rem 1rem 3rem;
    }
    .case-study__copy {
      position: relative;
      top: auto;
      left: auto;
      width: 100%;
    }
    .case-study__copy h2 {
      font-size: 4.5rem;
    }
    .case-study__scene {
      position: relative;
      inset: auto;
      height: 46vh;
      min-height: 22rem;
      margin-top: 2rem;
    }
    .product-plane {
      width: 100%;
    }
    .product-plane__bar {
      height: 2.2rem;
    }
    .case-study__depth {
      display: none;
    }
    .evidence {
      height: auto;
    }
    .evidence__sticky {
      position: relative;
      height: auto;
      padding: 6rem 1rem;
    }
    .evidence h2 {
      font-size: 3.8rem;
    }
    .evidence__scene {
      position: relative;
      inset: auto;
      display: grid;
      gap: 1rem;
      margin-top: 3rem;
      perspective: none;
    }
    .evidence-card {
      position: relative;
      top: auto;
      left: auto;
      width: 100%;
      transform: none !important;
      opacity: 1 !important;
    }
    .evidence-card figcaption {
      grid-template-columns: 1fr;
    }
    .journal {
      padding: 6rem 1rem;
    }
    .journal__intro {
      margin-bottom: 4rem;
    }
    .journal h2,
    .source h2,
    .finale h2 {
      font-size: 3.8rem;
    }
    .journal__column > a {
      grid-template-columns: 1fr auto;
    }
    .journal__column > a span {
      grid-column: 1 / -1;
    }
    .source__visual,
    .source__copy {
      padding: 3rem 1rem;
    }
    .finale {
      min-height: auto;
      padding: 6rem 1rem 5rem;
    }
    .finale__copy {
      width: 100%;
    }
    .finale__copy > div {
      flex-direction: column;
      align-items: flex-start;
    }
    .problem-portal {
      min-height: 30rem;
      margin-top: 2rem;
    }
    .problem-card {
      width: 74vw;
      min-height: 8rem;
    }
    .problem-card p {
      margin-top: 2rem;
      font-size: 0.95rem;
    }
    .problem-card--context {
      margin: -7rem 0 0 -2rem;
    }
    .problem-card--constraint {
      margin: 0 0 0 2rem;
    }
    .problem-card--outcome {
      margin: 7rem 0 0 -2rem;
    }
    .problem-card--ready {
      min-height: auto;
    }
    .finale footer span:nth-child(2) {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .visual-home *,
    .visual-home *::before,
    .visual-home *::after {
      scroll-behavior: auto !important;
      transition: none !important;
      animation: none !important;
    }
    [data-hero-stage],
    [data-case-screen],
    [data-case-copy],
    [data-depth-card],
    [data-problem-layer],
    [data-solution] {
      opacity: 1 !important;
    }
  }
</style>
