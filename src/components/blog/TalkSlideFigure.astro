---
interface Props {
  src: string
  alt: string
  caption?: string
  href?: string
}

const { alt, caption, href, src } = Astro.props
---

<figure class='talk-slide-figure not-prose'>
  {
    href ? (
      <a href={href} target='_blank' rel='noopener' aria-label={caption ?? alt}>
        <img src={src} alt={alt} loading='lazy' />
      </a>
    ) : (
      <img src={src} alt={alt} loading='lazy' />
    )
  }
  {caption && <figcaption>{caption}</figcaption>}
</figure>

<style>
  .talk-slide-figure {
    margin: 1.4rem 0 1.8rem;
  }

  .talk-slide-figure a {
    display: block;
    text-decoration: none;
  }

  .talk-slide-figure img {
    display: block;
    width: 100%;
    height: auto;
    border: 1px solid hsl(var(--border));
    border-radius: 8px;
    background: hsl(var(--muted));
    box-shadow: 0 16px 46px hsl(var(--foreground) / 0.12);
  }

  .talk-slide-figure figcaption {
    margin-top: 0.55rem;
    color: hsl(var(--muted-foreground));
    font-size: 0.86rem;
    line-height: 1.55;
    text-align: center;
  }
</style>
